server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    # You can include other configurations here, such as for handling 404 errors, compression, etc.
}
